| File          | Role                                                                 |
|---------------|----------------------------------------------------------------------|
| routing.ts    | Defines which locales exist and the default                          |
| navigation.ts | Creates locale-aware client-side navigation (Link, useRouter, etc.)  |
| request.ts    | Loads the correct translation file on the server for each request     |
